[
  [
    "CREATE TABLE IF NOT EXISTS DbVersion(versionId INTEGER PRIMARY KEY NOT NULL, version STRING)",
    "CREATE TABLE IF NOT EXISTS JsonData(dataId INTEGER PRIMARY KEY NOT NULL, dataBlob TEXT, ownerId INTEGER)",
    "CREATE TABLE IF NOT EXISTS User(userId INTEGER PRIMARY KEY NOT NULL, authToken TEXT)"
  ]
]
